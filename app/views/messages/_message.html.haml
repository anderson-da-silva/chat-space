.main__midle__message{data: {message: {id: message.id}}}
  .main__midle__message__upper
    .main__midle__message__upper__name
      = message.user.name
    .main__midle__message__upper__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .main__midle__message__lower
    - if message.content.present?
      %p.main__midle__message__lower__content
        = message.content
    = image_tag message.image.url, class: 'main__midle__message__lower__image' if message.image.present?